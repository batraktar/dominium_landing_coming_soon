window.innerWidth < 768 && [].slice.call(document.querySelectorAll("[data-bss-disabled-mobile]")).forEach((function (e) {
    e.classList.remove("animated"), e.removeAttribute("data-bss-hover-animate"), e.removeAttribute("data-aos"), e.removeAttribute("data-bss-parallax-bg"), e.removeAttribute("data-bss-scroll-zoom")
})), document.addEventListener("DOMContentLoaded", (function () {
    "AOS" in window && AOS.init()
}), !1), function () {
    "use strict";
    const e = document.querySelector(".mobile-nav-toggle");

    function t() {
        document.querySelector("body").classList.toggle("mobile-nav-active"), e.classList.toggle("bi-list"), e.classList.toggle("bi-x")
    }

    e.addEventListener("click", t), document.querySelectorAll("#navmenu a").forEach((e => {
        e.addEventListener("click", (() => {
            document.querySelector(".mobile-nav-active") && t()
        }))
    }));
    const o = document.querySelector("#preloader");
    o && window.addEventListener("load", (() => {
        o.remove()
    }));
    let n = document.querySelector(".scroll-top");

    function i() {
        n && (window.scrollY > 100 ? n.classList.add("active") : n.classList.remove("active"))
    }

    n.addEventListener("click", (e => {
        e.preventDefault(), window.scrollTo({top: 0, behavior: "smooth"})
    })), window.addEventListener("load", i), document.addEventListener("scroll", i), window.addEventListener("load", (function () {
        AOS.init({duration: 600, easing: "ease-in-out", once: !0, mirror: !1})
    })), document.addEventListener("DOMContentLoaded", (function () {
        const e = document.querySelectorAll(".service-title"), t = document.querySelectorAll(".service-card");
        e.forEach((e => {
            e.addEventListener("mouseenter", (e => {
                const o = e.target.getAttribute("data-target");
                t.forEach((e => e.classList.remove("active")));
                const n = document.getElementById(o);
                n && n.classList.add("active")
            }))
        }))
    }))
};